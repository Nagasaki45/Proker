# Proker functional tests

Browser automation test suite for Proker, written in python and playwright.

## Getting started

```
poetry install
poetry run playwright install
```

## Development

To run the tests:

```
poetry run pytest
```

To run in headed mode:

```
poetry run pytest --headed
```
